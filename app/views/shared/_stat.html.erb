<% @user ||= current_user %>
<div class="stats">
  <a href="<%=following_user_path(@user)%>">
    <strong id="following"class="stat">
      <%= @user.following.count %>
    </strong>
    following
  </a>
  <a href="<%=follers_user_path(@user)%>">
    <strong id="follwers" class="stat">
      <%= @user.follwers.count %>
      followers
  </a>
</div>  
